/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['static.vecteezy.com'], // Add the allowed image domains here
  },
};
